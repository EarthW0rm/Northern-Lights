version: '3'

services:
  clientgate.broker:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

  clientgate.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

